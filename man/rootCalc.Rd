% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rootCalc.R
\name{rootCalc}
\alias{rootCalc}
\title{Root Biomass Calculation}
\usage{
rootCalc(
  data,
  biomass,
  species,
  units,
  root_type = "total",
  output = "biomass Mg/ha",
  plot_radius = NULL
)
}
\arguments{
\item{data}{User specified dataframe.}

\item{biomass}{Column within data where pre-computed above-ground biomass is specified.}

\item{species}{Column within data where species is specified. NFI codes for species.}

\item{units}{Units of the biomass column. Must be specified: \code{"Mg/ha"} or \code{"kg"}.}

\item{root_type}{Which root fraction to return. One of "total" (default), "fine", or "coarse".}

\item{output}{One of "biomass Mg/ha" (default) or "carbon Mg/ha".}

\item{plot_radius}{Plot radius in metres. Required when \code{units = "kg"}.
Must be one of 3.99, 5.64, 7.98, or 11.28.}
}
\value{
A numeric vector.
}
\description{
Calculates below-ground (root) biomass or carbon using species-specific
root biomass equations from Li et al. (2003). Requires a pre-computed above-ground
biomass column. Specify the units of that column via \code{units}; if \code{"kg"},
a \code{plot_radius} is required to convert to Mg/ha before applying the equations.
Root decay is not applied and must be handled separately.
}
\examples{
trees_data$AGB <- treeCalc(trees_data, eval = "ung_2", species = "LGTREE_NFI",
  dbh = "DBH", height = "HEIGHT", appearance = "APPEARANCE",
  crown_cond = "CROWN_COND", output = "biomass Mg/ha", decay = TRUE)
rootCalc(trees_data, biomass = "AGB", species = "LGTREE_NFI", units = "Mg/ha")
}
